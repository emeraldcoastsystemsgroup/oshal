# CHANGE LOG
# -----------------------------------------------------------------------------
# SEQ                 | AUTHOR                      | DESCRIPTION
# -----------------------------------------------------------------------------
# 1 | maintainer@emeraldcoastsystemsgroup.com   | Initial — per-cluster tailnet relay: tailscaled (kernel mode, NET_ADMIN confined to THIS pod) + socat static forwards. main: exposes oshal-redis/oshal-api to the tailnet + dials remote bots. bot-pod: DNS-parity Services (oshal-redis/oshal-api/oshal-local-api) that dial the main relay + exposes local bots (5100-5199). Static forwards over subnet routes on purpose: enumerable in one values file + one ACL.
# 2 | maintainer@emeraldcoastsystemsgroup.com   | 0.2.0 — relay.enabled now defaults false (values.yaml): single-cluster tenants need none of this, and generic clusters may not permit NET_ADMIN + /dev/net/tun. Template unchanged; federation is a values flip away.
# 3 | maintainer@emeraldcoastsystemsgroup.com   | Production-readiness baseline (chart 0.5.0): pinned images (tailscale v1.102.4, socat 1.8.1.3, in values); RuntimeDefault seccomp; the forwarder runs as nobody (65534) with every capability dropped - it listens on unprivileged ports only; tailscale gets no privilege escalation but keeps NET_ADMIN on the runtime's default set (what kernel-mode tailscaled can lose from that set is unmeasured, and the pod's hostPath /dev/net/tun is outside every Pod Security level anyway); resources for both from values; the state claim honours storageClassName. No probes: neither process exposes a health endpoint. Guard: tests/unit/chart-production-baseline.spec.ts.
{{- if .Values.relay.enabled }}
{{- $ns := .Release.Namespace }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oshal-relay-state
  labels:
    {{- include "oshal.labels" . | nindent 4 }}
spec:
  accessModes: [ReadWriteOnce]
  {{- with include "oshal.storageClassName" (dict "root" $ "override" .Values.relay.stateStorageClassName) }}
  {{ . }}
  {{- end }}
  resources:
    requests:
      storage: 100Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oshal-relay
  labels:
    {{- include "oshal.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: oshal-relay
  template:
    metadata:
      labels:
        app.kubernetes.io/name: oshal-relay
        {{- include "oshal.labels" . | nindent 8 }}
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      securityContext:
        {{- include "oshal.podSeccomp" . | nindent 8 }}
      containers:
        - name: tailscale
          image: {{ .Values.relay.tailscaleImage }}
          env:
            - name: TS_AUTHKEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.tailnet.authKeySecret }}
                  key: TS_AUTHKEY
            - name: TS_STATE_DIR
              value: /var/lib/tailscale
            - name: TS_USERSPACE
              value: "false"
            - name: TS_AUTH_ONCE
              value: "true"
            - name: TS_HOSTNAME
              value: {{ include "oshal.tailnetHostname" . | quote }}
            - name: TS_EXTRA_ARGS
              value: "--login-server={{ .Values.tailnet.loginServer }} --advertise-tags={{ include "oshal.tailnetTag" . }}"
          # Kernel-mode tailscaled programs routes and netfilter as root with NET_ADMIN on top of
          # the runtime's default set; which of that default set it can lose has not been measured
          # on a cluster, so none is dropped here. The pod mounts /dev/net/tun from the host, which
          # no Pod Security Standard admits anyway (README "Pod Security").
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              add: ["NET_ADMIN"]
          resources:
            {{- toYaml .Values.relay.tailscaleResources | nindent 12 }}
          volumeMounts:
            - name: state
              mountPath: /var/lib/tailscale
            - name: devtun
              mountPath: /dev/net/tun
        - name: forwarder
          image: {{ .Values.relay.socatImage }}
          # socat listens on unprivileged ports only (6379, 5000, 5100-5199): alpine's nobody.
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            {{- include "oshal.minimalContainerSecurity" . | nindent 12 }}
          resources:
            {{- toYaml .Values.relay.forwarderResources | nindent 12 }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -x
              {{- if eq .Values.role "main" }}
              {{- range .Values.relay.inbound }}
              socat TCP-LISTEN:{{ .listenPort }},fork,reuseaddr TCP:{{ .service }}.{{ $ns }}.svc.cluster.local:{{ .port }} &
              {{- end }}
              {{- range .Values.relay.remoteBots }}
              socat TCP-LISTEN:{{ .listenPort }},fork,reuseaddr TCP:{{ .tailnetIP }}:{{ .listenPort }} &
              {{- end }}
              {{- else }}
              socat TCP-LISTEN:6379,fork,reuseaddr TCP:{{ required "tailnet.mainIP is required on bot-pod clusters" .Values.tailnet.mainIP }}:6379 &
              socat TCP-LISTEN:5000,fork,reuseaddr TCP:{{ .Values.tailnet.mainIP }}:5000 &
              {{- range .Values.relay.exposeBots }}
              socat TCP-LISTEN:{{ .listenPort }},fork,reuseaddr TCP:{{ .name }}.{{ $ns }}.svc.cluster.local:5000 &
              {{- end }}
              {{- end }}
              wait
      volumes:
        - name: state
          persistentVolumeClaim:
            claimName: oshal-relay-state
        - name: devtun
          hostPath:
            path: /dev/net/tun
            type: CharDevice
{{- if eq .Values.role "bot-pod" }}
---
# DNS parity on contributor clusters: bots resolve the SAME service names as in
# compose; these select the relay, whose socat dials the main cluster over the
# tailnet. Bots cannot tell they are remote — that is the contract.
apiVersion: v1
kind: Service
metadata:
  name: oshal-redis
  labels:
    {{- include "oshal.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: oshal-relay
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: oshal-api
  labels:
    {{- include "oshal.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: oshal-relay
  ports:
    - name: http
      port: 5000
      targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: oshal-local-api
  labels:
    {{- include "oshal.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: oshal-relay
  ports:
    - name: http
      port: 5000
      targetPort: 5000
{{- end }}
{{- if eq .Values.role "main" }}
{{- range .Values.relay.remoteBots }}
---
# Compose-parity DNS for a remote bot: the controller dials {{ .name }}:5000 exactly
# as if it were a local container; the relay carries it over the tailnet.
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  labels:
    app.kubernetes.io/part-of: oshal
    oshal.io/remote-bot: "true"
spec:
  selector:
    app.kubernetes.io/name: oshal-relay
  ports:
    - name: http
      port: 5000
      targetPort: {{ .listenPort }}
{{- end }}
{{- end }}
{{- end }}
