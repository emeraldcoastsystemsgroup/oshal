# Shared Workspace appearance and header navigation

The portal chooser now controls appearance across application navigation and open
tabs. Application colors remain available as an explicit option. Workspace uses
the existing page structures, with consistent shared cards, controls and text;
this is a selectable theme and navigation refinement.

Workspace links move into the desktop header beside the brand. Phones expose the
same authorized destinations through a Workspaces disclosure. The sidebar, active
document, unsaved content and existing authorization checks retain their behavior.

Core Users, Access, Applications, App Loader, Admin, Config Admin, AI Test Lab and
eighteen operations/studio pages now follow the shared theme bridge; Jarvis briefing
preferences also adopt the shared finish. Actual visual
review also corrected inverted Run Trace colors, fixed dark Queue panels and RAG
previews. Standalone chat follows saved changes without saving inherited bot colors.
All twelve palettes define native control schemes and primary accent text colors.

Career Hunter 1.20.0 coordinates thirteen existing screens, retaining white résumé
paper and all job/profile/application workflows. Its versioned Test Lab catalog
registers the new browser proof and existing suite groups with explicit runner
prerequisites. Create's home and new-design screens are also being verified against
the same portal palette contract. No business grants, providers or records change.

## Verification checkpoint

Source checks and deployment are recorded separately. Final core appearance
verification passed 225 cases across six files; a subsequent four-page shared
finish adoption passed 17 focused checks, including Jarvis briefing preferences.
All 71 existing palette, shared-style and static-route contracts pass. The header
suite passed 28 cases,
including 23 Chromium cases, four model cases and the Lab registration assertion.
Career passed 18 Chromium checks, 53 affected Node checks and the same 53 checks
through its catalog-selected sealed runner. The final RAG contrast regression
passes for Midnight, Daylight and Workspace. Career was published as `1eecfe1b`;
its immutable stage verifies 259 committed files plus provenance. Core typecheck
and scoped lint pass. Create compatibility checks are in progress; these counts
do not claim native acceptance or deployment. Publication and installation details
follow after the standard local rollout.
