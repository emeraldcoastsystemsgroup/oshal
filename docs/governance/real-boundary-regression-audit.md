# Real-boundary regression audit

This audit applies the integration-boundary corollary in `CLAUDE.md`: a test double may isolate
logic outside the defect, but closure evidence must execute the database, resolver, image, file, or
protocol seam that actually failed. “Real” here does not always mean a paid external account; a
real local HTTP server or Node resolver is sufficient for protocol/wiring claims. A production
provider claim still needs a separate live acceptance run.

## Dispositions

| Boundary audited | Mock/stub disposition | Required real companion | Status |
|---|---|---|---|
| Ticket-store writes under RLS | Unit router/gateway doubles stay for transition and error-branch coverage; they cannot prove PostgreSQL accepts or owns a row. | `tests/alert-intake-rls-live.spec.ts` and `tests/connector-webhook-rls-live.spec.ts` drive the real routers, `PostgresTicketStore`, GUC wrapper, NOBYPASSRLS role, policies, and rows. Both are mandatory in `tests/e2e-green-suite.txt`. | Real companion present. |
| Installed-package `@/` resolution | Route fixtures without framework imports stay for mount ordering/auth behavior; they no longer stand as alias evidence. | `tests/unit/manifest-route-mounter.spec.ts` registers the production `tsconfig-paths` hook and uses Node `createRequire` from an external temporary package to load a real module through `@/`. | Real companion present. |
| Package-facing build artifacts | Source/barrel checks remain useful but cannot prove the shipped image contains a module. | `scripts/check-kernel-skills.ts --image` inspects the built container; `tests/unit/kernel-uses-declaration.spec.ts` removes exact artifacts from a complete build fixture and requires the real gate to fail. | Real companion present. |
| Provider and connector gateways | Mocked fetch/provider specs are retained only for deterministic request shaping, status mapping, and retry branches. They may not close a “live provider works” outcome. | Local protocol-server tests close transport/wiring defects. Items explicitly requiring production OAuth, billing, hardware, or deployed-provider behavior remain in `docs/BACKLOG.md` until dated live evidence exists. | Explicitly separated; live outcomes remain queued. |
| `tests/unit/notify-channel-tier.spec.ts` (Notifications per-channel account tier) | Scoped doubles OUTSIDE the boundary: the pool answers the two existing connection probes (`gmailReady`, `twilioReady` - their SQL is unchanged and not what this claims), the fixed per-user Twilio operation is a recorder, and the Twilio vendor API is a fetch capture. | REAL: the `/api/notify` router over loopback HTTP, each sender's own `tier()` and the `available()`/`send()` branch that follows it (a reported `own` SMS reaches the per-user operation, a reported `deployment` SMS leaves from the deployment's number), and the page's shipped inline script executing under a minimal DOM with its fetch answered by that same route. Red on all 9 cases against unmodified main; mutation-checked by bare labels, a fixed label and a send branch decoupled from its tier. Not evidence that a real Twilio or Gmail account delivers. | Green 2026-09-15 |
| Demo-gated operator-key LLM lane | `tests/unit/operator-key-lane.spec.ts` stubs the vendor HTTP probe and the connections store. The defective boundary was the RESOLVER (`resolveUserLlmConnection` returning nothing for an operator, and the demo gate deciding whether a host key may be lent at all) — that seam runs for real in the spec, including the env switch, lane order, verdict cache, and non-operator refusal; the mutation check for the gate is documented in the header. | A live vendor completion on the lane the deployment actually runs (dated probe of the configured lane from inside the container) — the mocked fetch cannot prove a real key answers. | Real resolver covered; live-key acceptance is per-deployment and dated in the deploy record. |
| `tests/swarm-memory-rls-live.spec.ts` (durable swarm-memory provenance ledger — SEC-05, migration 117) | ONE scoped seam OUTSIDE the boundary: the vector index. `LiveMemoryRag` keeps the ingested documents in a `Map` so retrieval is deterministic — how ChromaDB ranks them is not this claim. Nothing else is doubled: no module mocking, no pool double, no fake identity rail. The pool-less service/route unit doubles stay for normalization and HTTP branches; they never proved migration 117 permits the intended owner flow or blocks a second owner. | REAL: a `pg.Pool` against a real PostgreSQL, with migration 117 applied verbatim into a unique schema the spec creates and drops; a NOSUPERUSER/NOBYPASSRLS probe role whose `rolsuper`, `rolbypassrls` and `current_user` the spec asserts before anything else, so the proof cannot go vacuous; the production `wrapPoolWithGuc` and `runWithRequestIdentity` stamping; loopback HTTP over the shipped `createMemoryRoutes` and the real `SwarmMemoryService`; two owners, a foreign-workspace read that must come back empty, operator promotion bound to the exact document SHA-256, a second owner's approval refused 403, and the durable rows read back through the broker. Mutation-proven twice against that real database: with the policy loosened to `USING (true)` the unbrokered non-bypass read returned **2 rows instead of 0**; with the service's transaction-local `set_config('oshal.swarm_memory_ledger_broker', 'on', true)` removed, PostgreSQL refused the ledger write and the store answered **200 instead of 201**. NOT evidence that the deployed `public.oshal_swarm_memory` carries this policy — the spec applies the migration into its own schema — and not evidence about vector retrieval. | Green 2026-09-17 (1 passed, 431 ms, PostgreSQL 16 in a disposable container on 127.0.0.1:55499, destroyed after the run). Registered in `tests/e2e-green-suite.txt`, and the runner is driven in the case above so the argv it hands playwright must be exactly that list. Its first run inside the gate is still owed, and is now diagnosed rather than merely outstanding: the `e2e-green` gate has not reached a single spec file since the SEC-05 line joined the list. The 2026-09-21 scheduled run ended at `Error: Timed out waiting 60000ms from config.webServer` 89 s in, and the four scheduled runs from 2026-09-18 on failed in 89-162 s - the webServer boot, not 72 spec files. That blocker belongs to the BACKLOG entry *Nightly gate has a twelve-night failure streak*, not to this boundary. |
| Dev-mode live-apply path confinement | Docker `restart` and the api's manifest-reload HTTP call are doubled in `tests/unit/live-apply-confinement.spec.ts` — they are effects OUTSIDE the boundary under test and are asserted on call shape only. | The defective boundary is the FILESYSTEM (a write escaping the checkout), so it runs for real: a temp repo, real `fs` writes, and a real directory symlink/Windows junction proving a lexically-confined path still resolves outside. Mutation-checked by removing the ancestor-realpath call — the junction case goes red. | Real boundary covered; container restart / manifest reload remain doubled and are exercised live by the dev-node itself. |
| Dev-mode promote (`scripts/oshal-deploy.sh`) | `tests/unit/deploy-promoter.spec.ts` injects the process spawner: a real run rebuilds an image and recreates ~34 containers and cannot live in a unit suite. | The exit-code CONTRACT is the defective boundary and runs for real as a pure function over 0/1/2/3/null/unknown; the script's own end of that contract stays pinned structurally by `tests/unit/deploy-rollback-outcome.spec.ts` (including a real `bash -n` parse). A live promote is dated in the deploy record. | Contract covered both sides; live promote is per-deployment and dated. |
| Hugging Face Inference Providers lane | `tests/unit/huggingface-lane.spec.ts` stubs the vendor HTTP probe (a canned 200 completion). The boundaries under test are the two lane TABLES (free-tier catalog and OpenAI-compatible operator lanes) and the operator-key RESOLVER, which run for real: one shared router base URL, `:cheapest` policy on every candidate, last-in-order placement, HF_TOKEN / HUGGINGFACE_API_KEY acceptance, and the probe URL + bearer header the resolver emits. | `scripts/evidence/prove-free-tier-live.ts` with `HF_TOKEN` set makes a real completion against `router.huggingface.co/v1` — the stub cannot prove a real token answers or that the `:cheapest` model ids are still served. No HF token was present on the operator box on 2026-09-04, so the live companion has not run yet. | Real tables + resolver covered; live-key acceptance pending an operator token, dated when run. |
| Kalshi live-order gate passthrough (compose → api env) | `tests/unit/compose-kalshi-live-gate.spec.ts` reads the compose file as TEXT: it pins the `x-bot-env` passthrough line, its `false` default, the api service inheriting the anchor, and the code-side `process.env.KALSHI_LIVE_ENABLED === 'true'` read. It cannot prove the interpolated container environment. | `docker compose -f docker-compose.oshal-local.yml config` must list `KALSHI_LIVE_ENABLED` under the api service (run 2026-09-04 at merge: resolved to `"false"` on every `*bot-env` service), and `docker exec oshal-local-api env | grep KALSHI_LIVE_ENABLED` after the next api recreate. | Static text + `compose config` covered; the in-container env probe is due at the next `oshal-deploy.sh` and dated in COLLABORATE.md when run. |
| `tests/unit/trading-settlement.spec.ts` (ADR-134 D8 cash settlement) | The VENUE is doubled at the engine's `getBrokerAdapter` seam — no Schwab call is made from a spec | REAL: the live Postgres (books/accounts join, the orders ledger, the `settlement_policy` CHECK) and the engine order path itself — both refusals are driven through `placeDecisionOrder` for an operator buy and an autonomous buy | Green 2026-09-06 |
| `tests/unit/trading-dispatch-golden-plan.spec.ts` (autopilot decomposition) | Scoped doubles OUTSIDE the boundary: the broker adapter, market data, the multi-timeframe scan, order placement and ticket creation are recorders/fixtures | REAL: the Postgres trading stores under the spec role — books, signals, decisions and orders through the real FK chain, plus peaks, daily equity, equity high-water mark, rotation state, pinned lots and config overrides. The claim is that the ordered dispatch plan and its persisted provenance rows are byte-identical across the split; it is not closure evidence for the venue or market-data boundaries | Green 2026-09-06 |
| `tests/unit/trading-watchdog-books.spec.ts` (ADR-134 D3.7 per-book beats) | None for the decision logic — the PowerShell functions are dot-sourced from the real script | REAL: `powershell.exe` 5.1 executing the shipped functions, the live Redis schedule store (seeded active/paused legs, cleaned up), the live Postgres roster read, and the cutover gate executed under `bash` with stubbed `docker`/`schtasks` for each refuse branch | Green 2026-09-06 |
| `tests/unit/trading-book-report-scripts.spec.ts` (ADR-134 D2 #7 per-book reports) | None for the SQL — it is the shipped module's own text | REAL: the live Postgres as the enforcing `oshal_app` role (self-validated: `current_user`, not superuser, not RLS-bypassing) with and without the operator GUC, plus real CLI runs of all three report scripts over seeded two-live-book data | Green 2026-09-06 |
| `tests/unit/trading-watchdog-checks.spec.ts` (ADR-134 D3.7 watchdog checks) | Scoped doubles: the Log/Raise sinks, and a stubbed exec wrapper in the core-hold case | REAL: the shipped PowerShell parsed and executed by `powershell.exe`, a real never-answering HTTP server for the wedge/deadline cases, and the pure check module itself — every threshold, alert key and message is mutation-proven | Green 2026-09-07 |
| `tests/unit/world-series-read-gate.spec.ts` (the market-hours pulse saturating the world series store) | Scoped double: the pg client. It records how many series reads it is asked to answer at once and returns canned rows; it says nothing about PostgreSQL's own behaviour under that load | REAL: the service's own `rollupFeatures`, its real statement text and the real process-wide gate — the measured quantity is the number of concurrent statements the pulse ISSUES, which is the quantity that saturated the store. Proven red before green by bypassing the gate (max in-flight 12 against a bound of 3; four sentiment statements for two answers). The read-cost half is separately evidenced by read-only `EXPLAIN (ANALYZE)` on the live `oshal_ts`: 786 ms planning + 366 ms execution on the stream vs 245 + 16 on the daily head | Statement-issuing boundary covered; the live companion is the pulse's own `seriesStatements`/`elapsedMs` record on the box after deploy, still to be taken |
| `tests/unit/multipart-request-identity-postgres.spec.ts` (multipart uploads losing the RLS request identity) | Scoped doubles OUTSIDE the boundary: the sign-in rail, and the domain service each post-upload handler calls (RAG ingest, the knowledge-memory record, the swarm-app loader, the ambient receipt store and diarization orchestrator, the agent-profile repository). Each double performs the handler's owner-scoped write as a real INSERT through the real GUC pool | REAL: loopback HTTP with the body streamed in 64 KB chunks with gaps, the four production routers and their multer parsers, the server.ts identity-middleware shape, the production GUC pool, and PostgreSQL FORCE RLS (the live owner-or-operator policy) evaluated for `oshal_app` in a throwaway database the spec creates and drops (self-validated: `current_user`, not superuser, not RLS-bypassing, and an identity-less write is refused). Red on all four routes before the fix and when the helper's re-bind is removed. Not evidence that each domain table's own policy is correct | Green 2026-09-14 |
| `tests/unit/cline-entrypoint-probe.spec.ts` and `tests/unit/cline-provider-fallback-model.spec.ts` (the Cline fallback brain: a glibc executable on a musl base, and the fleet model handed to a different backing provider) | Scoped doubles OUTSIDE the boundaries: the probe spec feeds the classifier reports captured VERBATIM from the broken image and a hot-fixed container and injects the docker runner only to assert the command shape; the provider spec replaces the wrapper's `executeTask` seam with a recorder and the model gate's export with a recorder. Neither doubles the launcher, the loader, or the resolver. | REAL: the artifact boundary is crossed by `scripts/check-cline-entrypoint.mjs`, which runs the real `cline` launcher inside `docker run`/`docker exec` and is gated in `scripts/oshal-deploy.sh` image verify (measured 2026-09-17: FAIL `glibc-binary-no-loader` on `oshal-bot:latest` and on `oshal-local-api`; PASS on a throwaway container carrying the new Dockerfile layer, where a real Gemini task also completed). The Dockerfile RUN body was executed in a throwaway container from the shipped image: exit 0 with the pinned version, exit 1 with a drifted one. The provider spec drives the real `ClineProvider`, the real wrapper resolver and a real `global-config.json` under the ADR-127 operator carve, including the non-operator refusal. Not evidence that a full image BUILD passes the in-layer asserts - no image was built on the box that day (memory constrained); the first `oshal-deploy.sh` run is that proof. | Artifact probe green/red on real artifacts 2026-09-17; in-build assert pending the first deploy |
| `tests/unit/multipart-request-identity.spec.ts` (the RUNNABLE half of the same guard) | ONE scoped double: the pg driver. A recording pool answers the stamp, the reset and the ambient advisory lock and captures the `oshal.current_sub` / `oshal.is_operator` values every other statement would have carried. What PostgreSQL DOES with that stamp is explicitly not this claim. The sign-in rail and the same domain services as the row above are doubled outside the boundary. | REAL: loopback HTTP with the body streamed in 64 KB chunks with gaps, the four production routers and their real multer parsers, the `server.ts` identity-middleware shape, `preserveRequestIdentity`, and the real `wrapPoolWithGuc` — which is the code that turns the AsyncLocalStorage identity into the stamp, so the measured quantity is the one that was lost. It exists because the PostgreSQL row above resolves its cluster through `specDatabaseUrl` and no gate sets `OSHAL_TEST_DSN`: that file reports `Tests no tests` and cannot tell a regression from a healthy tree. Mutation-proven — removing the helper's `runWithRequestIdentity` re-bind reds 4 of 5 (every route case; the unwrapped control stays green, which is what pins it as a control). NOT evidence that RLS refuses an identity-less write — that stays with the PostgreSQL companion. | Green 2026-09-16 (5 passed); runs in the default unit gate with no database and no Docker |
| `tests/unit/trading-settlement-autopilot-clamp.spec.ts` (ADR-134 D8 tail — the autopilot's settled-cash clamp) | Scoped doubles OUTSIDE the boundary: the venue adapter's `getAccount` (it answers the post-sell snapshot with the venue's own `accountType`/`settledCash`/`unsettledCash`; the Schwab adapter's mapping of those fields is not what this claims), market data, the `placeDecisionOrder` recorder, and a pool that answers the provenance INSERTs | REAL: `rotateSleeve` end to end (drop-out sell, settle wait, post-sell re-read, buy sizing), `capAccount` with the capital cap, and `settledBuyingPower`/`buildSettlementView`/`clampToSettled`. Red on 3 of 6 cases against unmodified main (the cash-book rotation funded both leaders from unsettled proceeds); red on 2 of 6 with the clamp placed before the cap. Not evidence that the Schwab venue reports those figures, nor of the engine's own refusal (that is `trading-settlement.spec.ts`) | Green 2026-09-15 |
| `tests/unit/bot-status-toggle-substrate.spec.ts` (the cockpit enable/disable toggle was compose-only) | Scoped doubles OUTSIDE the boundary: `node:child_process` — a real `docker compose stop` inside a unit run would stop a live container, so the command TEXT is what is asserted — `https.request`, so the Kubernetes API call is captured rather than sent, and an in-memory `agents` table underneath the real repository. | REAL: loopback HTTP, the shipped `/api/agents` router with its operator gate, `AgentStatusController`, `AgentProfileRepository`, `resolveBotRuntimeLauncher` and both launchers, and the dispatcher's own `normalizeCandidates` reading the row the toggle wrote. The live companion for the cluster seam is [`scripts/validate-dynamic-bot-manifest.mjs`](../../scripts/validate-dynamic-bot-manifest.mjs), which now also asks a reachable API server whether `deployments/scale` exists and accepts PATCH. Red on 7 of 7 against unmodified main; mutation-checked by rebuilding the compose pair inside the route (the in-pod toggle then issues one docker command) and by keying the toggle on the agent UUID (both compose cases go red). Not evidence that a real cluster scales the Deployment. | Green 2026-09-15; the live `deployments/scale` discovery check is due on the next reachable cluster |
| `tests/unit/oshal-app-build-staging.spec.ts` (a package build leaving sources in the kernel's `src/app/routes/`) | Scoped double OUTSIDE the boundary: the TypeScript compiler, in the two cases that need it stopped at a chosen moment — a stand-in `node_modules/typescript/bin/tsc` that records what was staged and blocks (the kill case) or exits non-zero (the failure case). What it compiles is not the claim. | REAL: the shipped `scripts/oshal-app.js build` against a real throwaway framework checkout on the real filesystem, killed with its whole process tree (`taskkill /T /F`), with every file under `src/app` sha256-hashed before and after; the success case runs the INSTALLED TypeScript compiler through the same path and proves the emit still carries `@/` from the new staging depth; and the killed run's survivor is handed to the real `scripts/check-repo-separation.js`, which refuses it. Red on 2 of 4 against unmodified main, whose failure output names `framework\src\app\routes\sports-routes.ts` as the destination it copied to. Not evidence about the store repo's own `rebuild-store-routes.mjs`. | Green 2026-09-16 |
| `tests/unit/send-message-budget-gate.spec.ts` (the INLINE half of `/api/send-message` cleared no admission gate - ADR-161) | Scoped doubles OUTSIDE the boundary: the pg driver, as a fake pool answering `BudgetService`'s own three statements (this box has no disposable Postgres and the live one is off limits), the ticket-context resolver, and the ADR-127 hosted-brain ladder's `resolveUserLlmConnection`, which serves one lane so a budget block cannot be confused with a missing brain. What PostgreSQL does with those statements is not the claim. | REAL: loopback HTTP, the shipped `createMessageRoutes` router and its whole inline branch, the real swarm bot registry (the controller-inline agent is FOUND in it, not hand-typed, so a bot changing container surfaces here), `assertBotInvocationAdmissible`, and the real `BudgetService` - its scope matching, spend comparison, hard/soft decision and fail-open semantics. The boundary that failed is route -> admission -> governance, and all of it runs. Red on 1 of 3 against the unmodified branch (an over-cap inline turn answered 200 and reached the orchestrator); the two allow/fail-open cases were green before and after, which is what pins the gate as a gate. Not evidence that `oshal_budgets`/`oshal_cost_events` are shaped as assumed - and explicitly NOT evidence that inline spend reaches the ledger those caps read (no inline path writes `oshal_cost_events`; that gap is its own BACKLOG entry). | Green 2026-09-16 |
| `tests/unit/trading-wash-sale-adapter-boundary.spec.ts` (the wash-sale stop veto and the ADR-159 withholding, measured from the VENUE ADAPTER) | Scoped double OUTSIDE the boundary: the pool, answering the engine's own filled orders for the book and an empty trailing-peak table. What PostgreSQL does with the ledger query is not this claim - it has its own real-PostgreSQL companion in `tests/unit/trading-engine-cost-basis-postgres.spec.ts`. | REAL: a recorded Schwab `?fields=positions` payload answered to the adapter's own `fetch`, `SchwabBrokerAdapter.getPositions()` and its `averagePrice` -> `avgEntryPrice` mapping, `withEngineCostBasis` over the positions that come out, and the fire's own `computeExits` (stop, take-profit, trailing and cap trim). Every existing exit spec hands `exitsToRun` a hand-built `Position`, so all of them stay green if that mapping changes and the veto is left measuring a number the adapter no longer produces. Green on 5 of 5 against unmodified main and mutation-proven: dropping the venue average from `toPosition` reds the mapping case, removing `washSaleStopVetoed` reds the veto case, and removing the `unmanaged` withholding from `exitsToRun`/`rebalanceTrims` reds two more. Not evidence that a live Schwab connection answers, nor about the store surface. | Green 2026-09-16 |
| `tests/unit/cli-token-schema-bootstrap.spec.ts` (the PAT-store boot bootstrap: one locked client, and a failure report that probed) | ONE scoped double: the `pg` driver. It records how many clients this code checks out of the pool, the statements it issues on each, and how it answers the post-failure table probe. It says nothing about what PostgreSQL does with those statements. | REAL: `ensureCliTokenSchema` and `createCliTokenRoutes` as shipped, the real `runRuntimeSchemaBootstrap` and `applyLockedSchema` (transaction, advisory lock, per-statement savepoints), the real `AsyncLocalStorage` request-identity sentinel the probe runs under, and the real fire-and-forget catch wiring. The measured quantity — acquires per bootstrap, and whether the handler looked before naming an impact — is the quantity that failed. Red on all 4 cases against unmodified main (0 client checkouts and 8 pool-level statements; 0 probes before the wording). **Not** evidence that a PAT authenticates or that the owner-RLS policy holds: per `docs/BACKLOG.md`, a fixture claiming either must connect as a non-superuser with the ADR-076 grants, because `postgres`/`oshal` bypass FORCE RLS. | Green 2026-09-15 |
| `tests/unit/swarm-cli-zsh-completion.spec.ts` (swarm-cli zsh completion) | Scoped double OUTSIDE the boundary: the controller's `GET /api/cli-tokens/whoami`, answered by a loopback stub so the real `swarm-cli login` can save two contexts. Inside zsh, only the final `compadd` is wrapped - it still matches through `builtin compadd -A`, then prints each match instead of inserting it | REAL: zsh - `zsh -n` over the script `swarm-cli completion zsh` prints, then compinit/compsys in an interactive zsh on a pseudo-terminal (zsh/zpty) with the script installed by `eval` and by `_swarm-cli` on `$fpath`, TAB via the complete-word widget; the saved contexts are read back by the completion's own `node` call from the file the CLI wrote. Mutation-checked: a dropped `revoke`, a broken autoload tail, a broken eval tail, a syntax error and a removed context completer each go red, and the pre-existing emit-only check stays green on the tail and syntax breaks. Proven on zsh 5.9.2 (MSYS2 build); fails, never skips, without a zsh | Green 2026-09-15 |
| `tests/unit/ci-local-secret-scan.spec.ts` (the `secret-scan` gate reporting PASS on a tree gitleaks could not fully read) | Scoped double ON the boundary that failed: the scanner image itself. The guard puts its own `docker` first on PATH and replays recorded stderr, so `zricethezav/gitleaks:latest` never runs, and the two paths its canned skip lines name (`/scan/config-seed/hidden.json`, `/scan/src/private`) are not in the three-file export the fixture builds. What it does prove, over the production `gate_secrets` body sliced out of `scripts/ci-local.sh`: `FAIL unread=2 of 3`, `FAIL unread=1 of 3` for the 2026-09-10 wording, `PASS unread=0`, `FAIL scanner rc=1`, and `ci-scan-src` purged on three of the four outcome paths (the 2026-09-10 wording case does not assert it). | REAL and present: a run of the production scan line against `zricethezav/gitleaks:latest` over an export holding a path the scanner cannot read, shown to FAIL - the row below, which also settles the floating-tag clause: the image the gate pulls still writes `skipping file: permission denied`, one of the five wordings in `GITLEAKS_UNREAD_PATTERN` (`scripts/ci/ci-secret-scan.sh`) calibrated against v8.30.1. | Real companion present as of 2026-09-21: `tests/unit/ci-local-secret-scan-unreadable-path.spec.ts` (row below) makes `zricethezav/gitleaks:latest` v8.30.1 skip a real path, exit 0, and the gate answer `FAIL unread=1 of 4`. The double stays for the wordings a denied file does not produce (`could not read file`, `skipping directory`, `cannot allocate memory`) and for the purge paths. The nightly's own counter agrees it is quiet: `%LOCALAPPDATA%\oshal\ci-local.log` carries `secret-scan: PASS unread=0 of 5598 exported files (scanner rc=0)` (2026-09-15) and `unread=0 of 5666` (2026-09-16), and no `secret-scan: FAIL unread=` line has ever been written there - `FAIL scanner rc=1` has, on four nights since 2026-09-17. |
| `tests/unit/ci-local-secret-scan-planted-fixture.spec.ts` (the `secret-scan` gate never proven to go RED on a credential, or GREEN once it was gone) | None on the boundary that failed. No stand-in `docker` is placed on PATH: `zricethezav/gitleaks:latest` really runs, under the production arguments, over a real `git archive` export of a disposable repository carrying this repo's real `.gitleaks.toml`. The one thing standing in for production is the SCANNED TREE - four commits of a four-file fixture rather than the ~5,600-file export the nightly scans - which is what lets the planted file be the single difference between the RED run and the GREEN runs either side of it. | REAL and present: the production `gate_secrets` body sliced out of `scripts/ci-local.sh` and run against the real image four times - clean `PASS unread=0 of 3`, planted `FAIL scanner rc=1 unread=0 of 4` alongside the image's own `leaks found: 1`, the allowlisted AWS documentation dummy back to `PASS unread=0 of 4` (so the RED is the planted value, not the shape or the extra file), and `PASS unread=0 of 3` once it is removed. | Green 2026-09-21 on `origin/main` 6011ceb6, image `v8.30.1`. Mutation-proven twice: `--exit-code 0` added to the scanner invocation, and the documentation-dummy allowlist widened from a value to a shape - each turns the planted stage green and the guard red. Evidence: [../security/secret-scan-planted-fixture-proof.md](../security/secret-scan-planted-fixture-proof.md). Does NOT cover the partial-scan half in the row above, the hosted Actions jobs, or any rule but `aws-access-token`. |
| `tests/unit/ci-local-secret-scan-unreadable-path.spec.ts` (the `secret-scan` gate never proven to refuse a PASS when the real scanner skipped a path) | None on the boundary that failed. No stand-in `docker` is placed on PATH: `zricethezav/gitleaks:latest` really runs, under `gitleaks_container_scan` sliced whole out of `scripts/ci-local.sh`, over an export built by the `git archive \| tar -x` line sliced out of `gate_secrets`, and the verdict is the `run_secret_scan "$exp" gitleaks_container_scan "$exp"` line sliced out of `gate_secrets` over the shipped `scripts/ci/ci-secret-scan.sh`. The SCANNED TREE stands in for production - four files rather than the ~5,900-file nightly export - which is what makes the denied path the only difference between the RED run and the GREEN runs either side of it. The `purge_tree` wrapper around those two lines is not re-executed here; the two rows above own it. | REAL and present: the image skips a path it cannot read and exits 0, and the gate refuses the PASS. A Windows deny-read ACE on one exported file is enforced by the host side of the read-only bind mount (`--wx-wx-wx` inside the container, `open()` returns `EACCES`); the guard reads the file back and THROWS if it is still readable, so it cannot pass for the wrong reason. Three scans of one export: readable `PASS unread=0 of 4 exported files (scanner rc=0)`, denied `FAIL unread=1 of 4 exported files (scanner rc=0)` beside the image's own `WRN skipping file: permission denied path=/scan/src/unreadable.ts` and `INF no leaks found`, then readable again `PASS unread=0 of 4`. The skip line is matched against `GITLEAKS_UNREAD_PATTERN` read out of the shipped helper, so a reworded floating `:latest` turns this red rather than passing a partial scan. | Green 2026-09-21 on `origin/main` dac39760, image `v8.30.1` (`zricethezav/gitleaks@sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f`), 6 passed in 21.89 s. Mutation-proven twice: `GITLEAKS_UNREAD_PATTERN` narrowed to `could not read file\|cannot allocate memory` (2 failed \| 4 passed), and the `[ "$unread" -gt 0 ]` refusal in `run_secret_scan` raised to `-gt 999` (1 failed \| 5 passed) - each turns the denied stage into `PASS unread=0 of 4`. Evidence: [../security/secret-scan-unreadable-path-proof.md](../security/secret-scan-unreadable-path-proof.md). Does NOT cover the hosted Actions jobs, the four wordings a denied file does not produce, or the nightly export itself. |
| `tests/unit/jarvis-surface-context.spec.ts` (the surface-bridge success-path log line — BUG-18's observability gap) | Scoped doubles OUTSIDE the boundary: the model (`executeBotOrInline`), the credential broker, the hosted-brain resolver, the user-model rails and the Postgres pool, plus the logger's DESTINATION — an in-memory buffer instead of the container's stdout/file transport. Where the bytes land is not the claim. | REAL: loopback HTTP, the shipped `createJarvisRoutes` `/ask` path and `extractSurfaceDirectives`, and the real pino pipeline the api logs through — built from the shipped `LOG_REDACT_OPTIONS` at the level the deployment runs the api at (the single `LOG_LEVEL` in `docker-compose.oshal-local.yml`'s `x-bot-env`, which `oshal-api` merges), with real level filtering, redaction, child `module` binding and NDJSON serialization. The case greps the emitted text exactly as an operator greps `docker logs`. Red on 2 of 25 with the route's log call removed, and red on 2 of 25 with the compose anchor set to `LOG_LEVEL: warn` — a mutation the previous hand-rolled logger recorder passed 24/24, because it could neither drop a line below a level nor censor a field. Red on 1 of 25 with `oshal-api` no longer merging the `*bot-env` anchor. Not an in-container observation: no running api was queried. | Green 2026-09-16 |
| `tests/unit/isolated-browser.spec.ts` (the owned fixture browser's cleanup deadline) | Scoped doubles OUTSIDE the boundary: a `BrowserServer`-shaped handle and fake timers, in the cases that must place an exit at a chosen instant - no real process can be made to exit at exactly 20 s, or never | REAL: headless Chromium behind the real Playwright `BrowserServer` for both halves - `launchIsolatedBrowser().close()` confirms a real owned process exited inside the budget, and a real server whose exit is not confirmed inside a 250 ms budget fails with the named deadline. The hook-budget pairing is read from the real spec files, and the eleven suites that own a fixture browser are the standing real companion. Red on 4 of 4 against the pre-fix fixture, whose first failure is the reported `Owned fixture browser 12345 did not terminate after explicit cleanup.` Five consecutive runs of `isolated-browser` + `jarvis-no-brain-browser` under a synthetic two-lane load passed 22 of 22 each while measuring forced cleanups of 17.6 / 12.6 / 7.5 / 8.5 / 5.5 s. TWO of those five - 17.6 and 12.6 - are demonstrable failures under the old bounds and past the runner's 10 s default as well; the other three are not, because the old path allowed a 5 s kill bound AND a 5 s exit bound after escalating, and `durationMs` measures to the exit event without recording the temp-directory cleanup that second bound covered. So the receipts show the budget is exercised well past 5 s under load, not that every one of them would have failed. Not evidence about any page those suites render. | Green 2026-09-16 |
| `tests/unit/app-home-plan-authorization.spec.ts` (Home offering an application the top navigation had already withdrawn) | Scoped doubles OUTSIDE the boundary: the installation repository (in-memory, so the suite touches no database), the identity provider (a fixture actor resolver), and the legacy coarse `AppAccessService.resolve`, which answers `admit`/`deny` on command. What PostgreSQL stores and how an OIDC session is established are not the claim. | REAL: loopback HTTP, the shipped `createSwarmAppRoutes` and `createWorkspaceNavigationRoutes` routers mounted side by side, the real `SwarmAppService` loading a real manifest off disk, and the real `ApplicationAuthorizationRuntime` + `ApplicationAuthorizationService` over a `MemoryAuthorizationStore` — grants applied through the real preview/apply path, so `canDiscover` and `protectedApp` actually execute. The boundary that failed is route -> application policy, and all of it runs. Red on 4 of 4 against unmodified main, where an ungranted caller received the full Home card for a protected package and kept it through revocation. Not evidence about the Home page's own rendering (that is `app-home-browser.spec.ts`) nor about any package's per-operation checks — discovery is coarse and authorizes nothing. | Green 2026-09-16 |
| `tests/unit/app-status-dashboard-route.spec.ts` + `tests/unit/app-status-dashboard-browser.spec.ts` (ADR-145 D3/D4 — the status dashboard for an APP, not only a group) | Scoped doubles OUTSIDE the boundary, all in `tests/fixtures/app-status-dashboard.ts`: the installation repository (in-memory records instead of Postgres — what PostgreSQL does with `swarm_applications` is not this claim), the `pg` driver behind the D5 `jarvis_tasks` read, and the caller's identity middleware. No resolver is doubled: the earlier ADR-145 harnesses stubbed `/api/swarm/apps/home-plan` with a hand-fed plan, which is exactly what this replaces. | REAL: one loopback express listener carrying the shipped `createSwarmAppRoutes` router, the real `SwarmAppService`, the real `getAppStatusPlan`, the shipped `src/pages/cockpit/tools/app-group-setup.html` served by the real `GET /:name/setup-dashboard`, the real `/shared/ui` assets, and synthetic packages answering at their OWN declared `mountPath`s. The plan is fetched over HTTP and the probe path THE PLAN NAMED is then fetched at that same origin. The browser half drives headless Chromium (isolated profile, loopback-only routing) against that page and asserts the render an operator sees: four tiles truncated from five, the same-app `fix` button kept and the foreign one dropped, the two readiness steps beneath them at `1 of 2 steps done`, and a deliberately broken probe (HTTP 500) painting `can't be checked right now` with no tile, no `tone-good` and no `step done`. Red on 11 of 12 against the unchanged route and page; the one green case is the 404 for an unknown name, which is unchanged behaviour. NOT evidence that a signed-in operator has seen this on the deployed box — that live check is its own BACKLOG entry. | Green 2026-09-16 |
| `tests/unit/app-registry-dns-fence.spec.ts` (ADR-147 D10 — a registry hostname that resolves into private space) | None on the boundary that failed. The two App Loader browser fixtures (`multi-store-routes-browser`, `app-dependencies-loader-browser`) DO hold scoped doubles outside it — `https.request` bridged to their own loopback express catalog, and the `DEFAULT_HOST_RESOLVER` seam answering a public address so those suites stay offline instead of asking the box's real DNS about github. Neither claims anything about the fence. | REAL: a loopback DNS server built in the spec (hand-encoded A/AAAA answers over UDP) read through a real node `dns.Resolver` pointed at it, driving the shipped `fetchRegistryCatalog` on both routes (raw-file API and `generic-git`); and a real `net` listener proving `pinnedLookup` sends `https.request` to the approved address under a hostname that does not resolve, with the un-pinned control failing `ENOTFOUND` at the same listener. The measured quantity — what the resolver answers and where the socket lands — is the quantity that was missing. Red on 7 of 15 against the pre-fix fence (`10.0.0.7` came back as `registry unreachable`, never refused) and red on the same 7 when the resolve step is removed. NOT evidence about the installer child's own clone, which is still unfenced (recorded in ADR-147's Not built), and not an in-container observation. | Green 2026-09-16 |
| `tests/unit/onnx-global-rethrow-handlers.spec.ts` (the ONNX wasm runtime's process-global rethrow listeners turning any stray rejection into an api exit) | No double on the boundary: the process listener registry, the ONNX runtime, `installProcessCrashGuards` and the stray rejection are all real, in real child processes. One case is NOT behavioural — a source-text pin that `local-embedding-service.ts` snapshots before the dynamic import and strips in `finally`. That pins WIRING only and is not closure evidence by itself; its real companion is the in-image case, which runs the real `load()`. | REAL: the installed `onnxruntime-web/dist/ort-web.node.js` (its own listener text is materialised and fed through the classifier the fix depends on); a real wasm init via a failing `InferenceSession.create(new Uint8Array([1,2,3,4,5,6,7,8]))`; the real crash guards; and an unawaited `Promise.reject` in a real child — survivable with the runtime absent, **fatal at exit 7** with it present and unstripped, survivable again after `stripRethrowGuards`. Plus two docker-gated cases inside `oshal-bot:latest`, where `Dockerfile.oshal:216-225` shims `onnxruntime-node` to `onnxruntime-web` and the defect is actually created: the real `load()` against the real model, pre-load 1/1 → 2/2 and exit 7 unfixed, 1/1 and exit 0 fixed, `embed(['x'])` = 384 dims both ways. The no-fix control is asserted, so a dependency that stops misbehaving fails the suite instead of passing it vacuously. NOT evidence about the running api container — no live process was observed, and the deploy-window `RestartCount` re-check is still owed. | Green 2026-09-16 |
| `tests/unit/local-embedding-abort-containment.spec.ts` (an Emscripten abort inside the wasm embedding runtime taking the api process down) | ONE scoped double, outside the boundary: the three service-policy cases replace the private `extractor` (and the in-flight case also sets the sticky flag directly, to stand in for a concurrent caller's abort) with a function that throws a real `WebAssembly.RuntimeError` once, to pin that an abort makes the service unavailable for the process and a transient error does not. What the runtime does is not their claim. | REAL: `tests/fixtures/onnx-abort-hook.ts` wraps `WebAssembly.instantiate` before the runtime initialises, keeps the runtime's own `_abort` import (`ort-web.node.js` key `B`, `function(){q("")}`) and raises it from inside the next wasm frame of a real run — the real onnxruntime-web runtime, the real quantized MiniLM `@xenova/transformers` caches after its first download (the package does not ship the weights; a fresh checkout fails loudly until the cache exists), the real crash guards and listeners, in a real `tsx` child. Contained (awaited, listeners stripped): exit 0, 10 bytes of stderr, listeners 1/1. Uncontained (unawaited, rethrow pair present): **exit 7**, asserted as the control so a runtime that stops aborting fails the suite. Docker-gated: the real `embed()` inside `oshal-bot:latest` — the aborting call null, the retry null with 0 wasm import calls, the service's own file-transport log line read back with `caller`, `count`, `chars`, `maxChars`, `batchStart`, `abort:true`; red on origin/main, where the retry re-entered the aborted runtime (261 import calls, a vector) and no abort line existed. Live half: `docker inspect oshal-local-api` RestartCount=0 from 2026-09-18T05:36:49Z through 07:41Z with the strip live, repeatable with `scripts/api-restart-count-probe.sh`. | Green 2026-09-18, host and image. |
| `tests/unit/ci-publish-image.spec.ts` (the image publish step pushing something it should not: a red run, a tag that is not the built commit, or a leaked credential) | `docker` is a recording stand-in first on PATH. The boundary the guard defends is the REFUSAL DECISION, and that runs for real - the shipped script, a real bash resolved through the production Git Bash probe, real argument parsing, real environment reads - with the stand-in only capturing what was asked of the registry. | A real push cannot run in a unit suite without publishing for real. The live companion is the first `--publish-image` run on the nightly gate, whose done-when is a fresh `--mode 1` install on a clean machine reporting `current` from scripts/image-freshness.js. | Recorded 2026-09-16; live companion OUTSTANDING (needs the operator-minted PAT). |
| `oshal-applications` sports-edge `sports_fantasy_player_weeks` (the scoring history a lineup's spreads are measured from) | Scoped double INSIDE the boundary: `sports-edge/tests/sports-fantasy-history.test.js` serves the table from memory, decoding the parameters the store's own `INSERT`/`SELECT` actually send. It proves the ROUTE writes the weeks and reads them back — which is what shipped broken — and it cannot prove PostgreSQL accepts the statements, because a double cannot refuse a bad one. | `sports-edge/tests/postgres/player-weeks-contract.mjs` runs the shipped migration, the runtime self-heal DDL over the migrated schema, the chunked upsert and its idempotence over a real primary key, the JSONB round trip and the read's season / week-exclusive / roster bounds, reading every row back over a second connection. | Real companion written and run (2026-09-16, disposable `postgres:16-alpine`); **OUTSTANDING: nothing runs it automatically** — see the section below. |
| `tests/unit/person-model-related-relevance.spec.ts` (ADR-100 — an off-topic line published as a "possibly related" recall hit) | Scoped doubles OUTSIDE the boundary, all inside `tests/fixtures/ambient-relevance-child.ts`: the Postgres pool (the three scoping reads `relatedRecall` makes — the `rag_chunks` presence probe, the spoken-name resolution and the owner time zone) and the retrieval leg, `RagService.prototype.search`, replayed at the exact reciprocal-rank scores and metadata the 2026-09-12 live proof observed. What Postgres stores and how pgvector ranks are not the claim; the claim is what the relevance rule does with a ranked list. The embedder is NOT doubled, deliberately — a stub can be made to rank anything and could not show whether "Can we order pizza tonight" is far enough from "volleyball". | REAL: the shipped `relatedRecall` and `applyRelevanceFloor` in a real `tsx` child process running the real `all-MiniLM-L6-v2` through `localEmbeddings` (the ESM-only dynamic import that cannot resolve under vitest's CJS transform, so the child is what makes the model reachable — the same reason `onnx-global-rethrow-handlers.spec.ts` uses one). Measured on the live-proof corpus for the query "volleyball": 0.391 the "net sport" paraphrase, 0.233 "knee pads", 0.169 the pizza line, against a control set never used to tune the floor at -0.048 / -0.030 / 0.145 / 0.044. Red on 3 of 11 against unmodified main, where the pizza line was published at a fused score of 1/63 beside 1/61 for a real paraphrase. The no-fix control (the same recall with the floor disarmed) is asserted, so a corpus or model that stops reproducing the defect fails the suite instead of passing it vacuously. NOT evidence from the deployed box: the live half is `scripts/person-model-live-proof.js`, which asserts the same separation against real pgvector and real Postgres and has not been run since the floor landed. | Green 2026-09-17; live companion OUTSTANDING (`scripts/person-model-live-proof.js` on a deploy). |
| `tests/unit/node-installer-posix-autostart.spec.ts` (the macOS/Linux one-click installer leaving no login item, so the node never came back after a restart) | Scoped doubles OUTSIDE the boundary: `node` and `npm` as stub executables first on `PATH` - the real `npm install -g @oshal/chat` downloads Electron, which is neither the claim nor something a test may do - and the `pg` driver in the route case. The stub does what the real one does as far as the script can tell: it creates the launcher in the global prefix and exits 0, so every line after it is the real thing running. What npm does with a package is not the claim. | REAL: GNU bash 5.2.37 EXECUTING the rendered script end to end, in a throwaway `HOME` under `os.tmpdir()`, with the login item read back off a real filesystem - the boundary that failed is the SHELL, because an installer is text until a shell runs it and text that reads correctly can still write nothing (a heredoc in the wrong place, a `set -e` that aborts before the write, a directory never created). One case fetches the script from the shipped `registerNodeInstallerRoute` over a real loopback Express mount and runs THAT, so the renderer being right cannot stand in for the route serving it. Also measured: the entry names the launcher npm actually created (not a hoped-for name), `XDG_CONFIG_HOME` is honoured on Linux, the entry carries no token or control-plane URL, and a `mkdir -p` that cannot succeed reports "will NOT restart" while the node still starts and the script still exits 0. Red on 6 of 7 against unchanged `main`; the seventh asserts bash is present at all and fails loudly rather than skipping. NOT evidence that a real Mac or Linux machine enrolled - that bare-machine run is still owed in `docs/BACKLOG.md`, and this suite runs on MSYS bash, not on macOS's bash 3.2 or a systemd desktop. | Green 2026-09-17 |
| `tests/unit/series-storyboard-owner-sub.spec.ts` (Video Studio series storyboards dying at the ADR-130 demo carve) | Scoped doubles OUTSIDE the boundary: the `pg` driver (an in-memory router answering the statements this stage issues — what PostgreSQL does with them is not the claim) and the Drive upload, which a unit run must never make. The bot-node executor is doubled ON its seam by design: it IS the app-boot injection point ADR-130 defines, so the bot-side SEC-05 spawn is explicitly NOT evidence here. | REAL: `advanceVideoSeries`, `storyboardEpisode`, `generateEpisodeStoryboard`/`generateStoryboardFrame` with their real PNG decode, white-margin crop and 16×16 average-hash distinctness check, `resolveStoryboardImageProvider` with its demo-aware default, the real `codex-cli` provider, and the real shared-workspace filesystem (the scene-1 anchor staged on disk for scene 2, `output.png` read back and magic-checked). The measured quantity is the sub that ARRIVES at the executor — the one that was missing. Red 2 of 3 against unmodified main (`storyboard image provider 'codex-cli' is not configured`, thrown from the resolver down the conductor → pipeline → frames stack); the third case is the control and the anti-hardcode mutation check — a non-operator owner still fails closed and reaches no executor. | Green 2026-09-17 (3 passed; 8.0 s per rendering case, the stage's real inter-scene pacing). The live demo-box render through a real bot node is owed and stays in `docs/BACKLOG.md`. |
| `tests/unit/node-enrolment-device-binding.spec.ts` (the credential `POST /api/join/enroll` hands a new computer) | Scoped double OUTSIDE the boundary: PostgreSQL itself, through `tests/helpers/fake-cli-token-pool.ts` — `oshal_cli_tokens` rows held in an array behind the store's OWN statement text, so an unmodelled statement throws instead of answering zero rows. What Postgres does with RLS, types and constraints is not this claim; `tests/unit/cli-token-*` and the RLS suites are where that lives. The enrolling human's OIDC session is a stamped `req.oidc`, exactly as `remote-client-node-token.spec.ts` stamps it. | REAL: one loopback express listener mounting, in server.ts's order, the shipped `createCliTokenAuthMiddleware`, the shipped `createJoinRoutes` (minting through the real `insertCliToken`) and the shipped `createRemoteClientRoutes`. The token the enrol route mints is carried back over HTTP as a `Bearer` and registered — so the round trip that used to end in 403 `node_token_client_mismatch`, or in an unbound hour-long PAT, is executed rather than asserted, under `REMOTE_CLIENT_REQUIRE_NODE_TOKEN=true` with no `REMOTE_CLIENT_SHARED_SECRET` set anywhere. The reach case fetches an ordinary account route with the same credential and measures 401. Red on 10 of 11 against the unchanged source; the one green case (a node cannot mint a second device's credential) is a no-regression pin on middleware this change does not touch. NOT evidence that a machine with no checkout has enrolled — that bare-machine proof is the remaining half of its BACKLOG entry and needs real hardware. | Green 2026-09-17 |## Configurable Home (2026-09-09)
| `tests/unit/bot-node-database-pool-recovery.spec.ts` (a bot that loses the cold-start race to Postgres is pool-less for life and reports healthy) | None inside the boundary. `tests/unit/bot-node-application-authorization.spec.ts` doubles the ownership reader to pin the CODE each refusal shape carries (missing pool, wrapped socket refusal, 42501, conflict); that file is branch coverage only. | REAL: the socket — `connectRecoverableBotNodeDatabase` runs against a reserved loopback port with nothing listening, exhausts its boot window, and a real `postgres:16-alpine` is started on that port only afterwards; the real `/health` + `/api/health` routes over loopback HTTP probed by `curl -f` exactly as `Dockerfile.oshal`'s HEALTHCHECK does (exit 22, then 0); the real posture guard with the real ownership reader over that pool (refuses `database_pool_unavailable` while the socket is dead, returns to `authorization_bot_posture_unavailable` once the database answers with a missing-helper error); the one-shot `connectPool` still returning null. The container is owned by the spec and force-removed. Not evidence of a built image's healthcheck (no image build was run) nor of a full `bot-node-server` boot (Redis is not part of the fixture). | Green 2026-09-17 (7 passed, ~14 s) |
| `tests/unit/provider-switch-store-postgres.spec.ts` ("a bot's LLM provider is a row in a table": the operator-written per-bot rows and the fleet-default row of migration 147, read through one snapshot; the `agent_config` record is beneath both) | Scoped doubles OUTSIDE the boundary live in the sibling specs only: `provider-switch-routes.spec.ts` and `provider-switch-cockpit-browser.spec.ts` double the switch store and the config-sync push (both doubles now carry `fallbackOrder` with the real store's semantics - `undefined` keeps what is stored, an array replaces it; the route double silently dropped that argument from migration 148 until 2026-09-18, so nothing in that suite could observe what the route writes for a chain) (what a bot does with a push is the bot-node's own guard, `bot-node-provider-switch.spec.ts`), `config-runtime-precedence.spec.ts` doubles the store's per-bot write seam (`writeBotSwitch`), and `harness-resolution.spec.ts` and `dispatch-switch-row-stamping.spec.ts` install an in-memory snapshot (over the real rule, real catalog and real registry) to measure `resolveHarnessForAgent` and tier-1 dispatch stamping respectively. None of them claims the database. | REAL: a disposable PostgreSQL 16 the spec starts and force-removes (`tests/helpers/disposable-postgres.ts`, loopback port invented at start — it cannot reach the deployment), migrations 001 + 011 + 147 + 148 as shipped (148 adds the `fallback_order` column and its subquery-free CHECK), the real `ProviderSwitchStore` (reading ONLY `oshal_bot_provider_switch`; `agent_config` is never projected), the real `ProviderSwitchSnapshot`, the real `AgentConfigService` and the real `createAgentConfigRuntimeParamsResolver` (tier-1 dispatch stamping over this pool), and the real GUC pool wrapper — as the ENFORCING `oshal_app` role (self-validated in the first case: `current_user`, not superuser, not RLS-bypassing, table owner under FORCE RLS). Proven: an operator identity writes the fleet row and a per-bot row, and a non-operator is refused by the table's own policy (42501) for either while still reading; the CHECKs refuse a blank scope, `auto`, a provider with whitespace and a blank model, and - since migration 148 - a `fallback_order` entry that is blank, null or contains whitespace (the CHECK had to be written subquery-free: PostgreSQL refuses a subquery in a CHECK constraint, which only this real-boundary spec could have caught); an `agent_config` record — even one tagged with an operator-looking `configUpdatedBy` — is NOT a switch row; per-bot > fleet > registry from the rows PostgreSQL holds; a hand-written unrunnable id is refused by name at resolution; clearing falls back rung by rung; a failed refresh keeps the last good rows. Then the operator box's own row shape (`tests/fixtures/agent-config-provider-rows-2026-09-17.json`: 70 `agent_config` records with blank / `bot-local` / `oshal-push` `configUpdatedBy`, zero switch rows, package bots registered into the active registry as the app loader does): zero rows = the registry rung and the record byte for byte; ONE fleet-default write moves all 70 in the resolver and the stamped dispatch record, registry and package bots alike, and a second write moves them again; an operator-written per-bot row beats the fleet row for that bot only; clearing returns all 70. Red on the pre-fix store (9 of 15: every bot answered `bot-row` with its `agent_config` provider after the fleet write). Not evidence that the bot-node runs the row (that is the bot-node guard). | Green 2026-09-18 (16 passed, 24.8s) |
| `tests/jarvis-rich-response-native-wake.spec.ts` (the unthemed Jarvis page killing its own microphone and native-wake handlers - BACKLOG "Native background wake: the microphone-release invariants are unproven") | Scoped doubles OUTSIDE the boundary: `getUserMedia`, `AudioContext`, `MediaRecorder` and `speechSynthesis` are recording fakes (so a released track and a closed context are observable without a physical microphone), and every backing API route answers canned JSON. Neither is the claim. | REAL: the shipped `src/api/jarvis.html` executed by Chromium with a real 2D canvas. The theme assets (`surface-themes.css`, `surface-theme.js`, `/cockpit/css/themes/*.css`) are withheld with 404s, which is how a failed stylesheet load presents; the first synchronous `tick()` then runs the orb's fallback colour through the real `CanvasGradient.addColorStop`, and the wake event must reach a listener that only exists if the script survived. The case also asserts zero `pageerror`s. Red on the `origin/main` page (the fallback string threw and `__OSHAL_JARVIS_NATIVE_WAKE_READY__` stayed unset); the three sibling cases went 3 failed / 3 → 3 passed / 3 by serving the theme files off disk to the UNCHANGED page, which is what proved the red was fixture drift. Not physical-device sign-off: no real microphone was opened. | Green 2026-09-18 |
| `tests/unit/pool-connection-errors.spec.ts` + `tests/unit/api-storm-probe.spec.ts` (BACKLOG "Deploy — the api process exits during the bot-recreate storm": a server-terminated checked-out pg connection was an uncaught exception and, through the crash guards, the end of the api process) | None inside either boundary. `tests/unit/deploy-promoter.spec.ts` decodes the new exit 6 as a pure function, which is the contract and not the engine. | REAL: (1) the pg driver's connection-error routing under a real server-side session termination — a disposable `postgres:16-alpine` (`tests/helpers/disposable-postgres.ts`) with `idle_in_transaction_session_timeout` set on the database, the real `pg` Pool, the real `installProcessCrashGuards`, in a child process (`tests/fixtures/pool-connection-errors-child.ts`) whose exit code is the verdict, with the server's own log read back for its `terminating connection due to idle-in-transaction timeout` line each time. Unowned: exit 1, `UNCAUGHT EXCEPTION`, the 2026-09-05 message verbatim. Owned (`ownPoolConnectionErrors`): exit 0, an ERROR line per pg error event (two for one termination: the server’s 25P03 and pg’s own connection-terminated event, milliseconds apart) with the backend pid, `COMMIT` rejected `not queryable`, the pool still answering; an idle client killed with `pg_terminate_backend` surfaces once on the pool's `error` event and the pool still answers. (2) the Docker engine the deploy probe reads — real `docker inspect` RestartCount and `docker logs --since`, the real script in a real Git Bash, on disposable alpine containers the spec owns: one stays up (PASS), one Docker restarts inside the window (FAIL), one writes the termination line inside the window (FAIL), one does not exist (exit 2, never PASS). **The deploy run has now happened.** `scripts/oshal-deploy.sh` recreated the api and 36 bots in batches of 5 on 2026-09-19, and the probe ran inside that window as the gate it was written to be:
`[12:05:03] api-storm-probe: oshal-local-api RestartCount 0 -> 0; 'idle-in-transaction' log lines since 2026-09-19T15:51:31Z: 0` / `[12:05:03] api-storm-probe: PASS - RestartCount unchanged and no 'idle-in-transaction' line in the window`. That is the entry's done-when, met: the api held through the full bot-recreate storm that used to end its process. Worth noting what it does NOT prove - the window saw no server-side termination at all, so this is the api surviving a storm, not the ownership handler being exercised by one. The handler's evidence remains the disposable-Postgres child, where a real `idle_in_transaction_session_timeout` termination is forced and the exit code is the verdict. | Green 2026-09-18 (3 passed, ~9 s; 4 passed, ~15 s); **deploy run PASS 2026-09-19** |

| `tests/unit/install-owner-grant-postgres.spec.ts` (the install owner's automatic `admin` tier on every application an install staged) | Scoped double OUTSIDE this boundary: `tests/unit/install-owner-stamp.spec.ts` keeps its `vi.fn()` Pool for the branch logic — which tier is asked for, when adoption fires, that it never throws. That file is branch coverage and is NOT closure evidence for the database, and its decisive line reads the SQL TEXT for `ON CONFLICT ... DO NOTHING` on a statement nothing executes. | REAL: a disposable `postgres:16-alpine` the spec starts and removes, migrations 022/121/145/146 as shipped, a NOSUPERUSER NOBYPASSRLS role behind the production GUC wrapper, and the real `AppAccessService`. Self-validated twice before anything is claimed — `rolsuper` and `rolbypassrls` are both asserted false, and a NON-operator write is refused by `oshal_app_access_operator_write` with SQLSTATE 42501 — so the fixture enforces rather than agreeing with itself. Proven: the grant lands for a principal with no tier; a second grant writes nothing and reports false, which is what lets adoption run on every boot; and an explicit `deny` someone set on purpose SURVIVES it. Mutation-proven — `DO NOTHING` changed to `DO UPDATE SET tier` turns 2 of 5 red, including the deny case. NOT evidence about the pre-145 fallback (`insertIfAbsentWithoutIssuer`), which still has no real-boundary case: it is reached only when the `user_issuer` column is absent, and this fixture runs 145. | Green 2026-09-19 |
| `tests/unit/trading-market-data-rate-limit.spec.ts` + `tests/unit/trading-assess-step-failure.spec.ts` (the nightly assessment losing its per-algo record to a market-data rate limit) | Scoped doubles OUTSIDE the boundary in the step-attribution spec: the market-data layer, the schema/resolver helpers, the world-intelligence service and the pool are recorders, because that spec claims only that a failed stage is named and does not abort the stages independent of it. | REAL: the rate-limit spec drives the shipped `barsBatch` over the module's own HTTP seam (`globalThis.fetch`), answering with Alpaca's documented refusal for `GET /v2/stocks/bars` — 429 plus `X-RateLimit-Limit` / `X-RateLimit-Remaining` / `X-RateLimit-Reset` — so the pagination loop, `adata()`, the typed 429, the bounded retry and the shared quota window all execute unmodified. Mutation-proven both ways: reverting `adata()` to its pre-fix shape turns 4 of 6 cases red (the paginated batch raises `too many requests.`), and reverting the dispatch to its single `try` turns 2 of 3 step cases red. NOT evidence that the live Alpaca account answers, and not evidence that two consecutive nightly assessments run clean — that clause is live proof owed on the box. | Green 2026-09-21 |
| `tests/unit/ambient-test-fixture-routes.spec.ts` + `tests/unit/ambient-test-fixture-postgres.spec.ts` (ADR-100: the AI Test Lab could seed only UNATTRIBUTED lines, because `POST /api/jarvis/ambient/segments` refuses speaker ids by design and the only attributed writer was the trusted audio pipeline — so asks, per-person profiles and consent were unprovable without a microphone) | Scoped doubles OUTSIDE the boundary live in the gate spec only: it doubles `AmbientListeningService.appendAttributedSegments`/`getSettings`, `SpeakerProfileStore.identify`/`assignProfile` and `enrichBatch`, because its claim is about the GATE and about WHOSE store is written, not about rows. It is not closure evidence for the database. The one double present in BOTH specs is the ambient-analyst's reply, which the fixture supplies deterministically so the Lab spends no LLM; a live analyst run stays owed by the separate backlog entry "Ambient Recall (ADR-100) — live acceptance on real transcripts". | REAL: (1) the gate itself — the shipped router with the shipped `requireServiceSecret` on a real HTTP listener, so 401 without a secret, 401 on a wrong secret, 503 on an unconfigured deployment and 401 for a secret with no session are observed rather than asserted about source. (2) the rows — `ambient-test-fixture-postgres.spec.ts` starts a disposable `postgres:16-alpine` (`tests/helpers/disposable-postgres.ts`; the address is invented at start() so it cannot reach the deployment), applies the deployment's own migration chain (068 ambient, 069 speakers, 138 parity) plus the lazy `personModelSchemaStatements()`, and drives the same router over HTTP with the REAL `AmbientListeningService`, the REAL `SpeakerProfileStore` (real embedding encryption) and the REAL `enrichBatch`. Proven on those rows: the segment carries a `speaker_profile_id`, the consent ledger holds one granted transcript-scope row, the ask is written under `model = 'test-lab-fixture'`, and the production reads `getOpenAsks` and `personProfileSummary` return it with its label, topic, presence and verbatim quote; a replayed token duplicates nothing; a second run matches the SAME per-owner fixture voice; a second owner gets their own voice and cannot read the first's profile. Mutation-proven both ways: removing `router.use(requireServiceSecret)` turns 3 of 9 gate cases red (201 where 401/503 is required), and reverting the fixture to the unattributed path (`speakerProfileId: null`, `profileId: null`) turns 4 of 5 Postgres cases red — zero asks, a null profile summary. NOT evidence that the ambient-analyst bot produces this ask on real speech, and not a Lab run: the scenario steps are registered but reporting pass on a deployed box is live proof still owed. | Green 2026-09-21 (9 passed; 5 passed) |
| `tests/unit/device-ownership-visible.spec.ts` (BACKLOG "Node enrolment is the blocker for every remote-node capability": a node's enrolment is judged on coming up BOUND to a person, and the only cockpit surface listing computers showed name, status and heartbeat - so an enrolled node and one bound to nobody rendered identically) | Scoped double OUTSIDE the boundary: the OIDC session is a header/cookie-driven stand-in, the task journal is the in-memory fixture, and the BROWSER is a minimal fake DOM (this repo carries no jsdom) exposing only what a browser would. | REAL: the `/api/remote-clients` router over loopback HTTP with the real `RemoteClientRegistryService` and the real `canAccessResource`/`describeDeviceOwnership`; the shipped `src/pages/cockpit/tools/devices.html` inline script executing against the VERBATIM bytes that route produced; and the registered Test Lab card's own step run against the same router. Mutation-checked: reverting the route leaves 5 of 9 red, reverting the page 2 of 2 page cases, and defaulting an absent binding block to owned turns the fail-closed case red. Not evidence that a real browser paints it, and not evidence that a bare machine enrols - the done-when's hardware clause stays open. | Green 2026-09-21 |


`tests/unit/app-home-customization.spec.ts` uses a recording pool only for HTTP input/owner binding and failure branches. Its real companion is the store's `identity/tests/home-summary.integration.cjs`: actual PostgreSQL with the canonical owner-RLS policy and a non-superuser role, the production GUC wrapper and preference queries, the compiled Identity extractor and real accessible-connections helper, plus Chromium on the actual Home module/CSS. Mock authentication and seeded source records remain intentional boundaries; this is not live-provider acceptance. The harness rejects nonlocal/non-test database names and removes only its generated schema/role. Passed for owner isolation, stale revisions, shared-account scope, renewable expiry, unavailable-source failures, keyboard editing and desktop/mobile rendering.

## Core/store compatibility gate (2026-09-10)

`tests/unit/store-compatibility.spec.ts` uses real disposable Git repositories, the shipped store
compiler and the installed TypeScript compiler; none of those boundaries are mocked. It proves
committed-only selection despite dirty/untracked source, TS2305 rejection of a consumer whose
ambient stub invents a core export, dependency-lock mismatch refusal, normal cleanup, and source
repository preservation when the compiler process tree is forcibly interrupted.

The full exported pair `9655afde416e09058562cba0b965521bff1a0550` (core) and
`f5db2153a25d8ebd18709dec10a03b130dac559c` (applications) passed on 2026-09-10:
386 sources across 52 packages, followed by a failing real TS2305 compile naming
`compatibility-negative-probe/probe.ts` and `inventedCompatibilityExport`. These are dated
compiler-produced counts, not an inventory constant. The first acceptance run explicitly reused
manifest/lock-matching provisioned dependencies. A second full run installed fresh dependencies
with `npm ci` from the pinned core lockfile and passed with the same counts; both exports cleaned
successfully. Per-run logs and the two SHAs are retained by
[`check-store-compatibility.mjs`](../../scripts/check-store-compatibility.mjs); invocation and
scope are documented in [the local CI runbook](../runbooks/local-ci.md#corestore-compatibility-release-check).
This attests to source compatibility, not legacy JavaScript behavior or emitted-output parity.

## Shared artifact picker (2026-09-10)

`tests/unit/artifact-picker.spec.ts` runs the actual framework artifact/file routers, local
owner-hashed storage directories, short-lived handles, loopback byte redemption and Chromium on
the shipped Portrait Studio surface and shared picker. It also loads the real Portrait Studio
source route. Authentication and the portrait SQL store are explicit fixtures; this does not
claim PostgreSQL/RLS or external connector acceptance. No compiler, filesystem, picker or handle
redemption implementation is replaced.

The local proof passes for registered/visible source discovery, removal on unregister, MIME
filtering, invalid navigation, owner-only listings, foreign-handle refusal, byte-identical file
redemption, folder/back navigation, zero mints on cancellation, rejection of external source URLs,
late-response cancellation, and selecting both a local file and a gallery image into the actual
crop stage. No generation or publication is triggered. Mobile width and light/dark theme changes
pass. The related artifact/auth suites pass together (34 tests); Portrait Studio's standalone
camera proof passes 23 checks. Run locally with `node node_modules/vitest/vitest.mjs run
tests/unit/artifact-picker.spec.ts`; set `OSHAL_STORE_REPO` when the repositories are not siblings.
The deployed signed-in acceptance remains in BACKLOG until the protected core change lands.

## Jarvis YAML routing and artifact handoff (2026-09-10)

`tests/unit/jarvis-tool-catalog.spec.ts` loads the shipped YAML and mutates invalid metadata and
role grants. `tests/unit/jarvis-artifact-routing.spec.ts` exercises actual registry and owner-bound
handle selection, post-model expiry/visibility checks, and the real Jarvis ask/result HTTP path.
Its model and persistence are explicit fixtures; no live deployment database is used.

`tests/unit/artifact-dispatch-browser.spec.ts` runs Chromium with the shipped dispatcher and Jarvis
surface, actual artifact HTTP routes and registry, and explicit authentication/model-result/source
fixtures. It checks same-ref dispatch, confirmation refusal, foreign handles, removed destinations,
and stale selection. This does not prove deployed model disambiguation. Keep ADR-139 Stage 4b open
until a signed-in live model handoff and ambiguous-target refusal are recorded after deployment.

## Swarm administration route chain (2026-09-14)

`tests/unit/swarm-admin-route-chain-authorization.spec.ts` closes the ADR-148 open thread "no
route-chain guard for the 403 path". The boundary that had no coverage is the CHAIN, not any one
function: the shipped guards proved the role store and `isOperatorIdentity` separately, and the
signed-in non-operator 403 existed only as a hand check on a box. The spec runs the whole chain for
real — the deployment auth set from `createApplicationAuthMiddlewareSet` in its LOCAL_AUTH shape, a
session cookie minted by the real `POST /api/local-auth/login`, that set's own `requiresAuth`,
`requiresOperator`, `isOperatorIdentity`, the privileged-identity snapshot, a real `swarm_roles`
table in a disposable PostgreSQL container, and the shipped `/api/swarm/roles` and
`/api/swarm/registries` routers. Both break-glass allowlists are stubbed empty, so a row is the
only path to operator; the same account is asserted 403 before the grant, 200 after it, and 403
again after the revoke.

The ONE scoped double is `SwarmAppService.loadApp`, injected into the registry router. It is
outside the boundary (it installs a package) and throws if anything calls it, so no assertion can
reach it. Its real companions are `tests/unit/multi-store-installer.spec.ts` and
`tests/unit/app-dependencies-loader-browser.spec.ts`.

Every outcome is asserted on the RESPONSE BODY, never the status alone: on a live box every
unauthenticated `/api/*` path answers an identical 401, including paths that do not exist, so a
status-only assertion proves the global guard and not the mount. Mutation-proven both ways on
2026-09-14 — making `requiresOperator` always call `next()` turned 3 of 4 red (the non-operator
case first, "expected 200 to be 403"), and dropping the `swarm_roles` snapshot consult from
`isOperatorIdentity` turned 2 of 4 red ("expected 403 to be 200") while leaving the anonymous and
non-operator refusals green. This is not evidence for RLS: `swarm_roles` deliberately has no row
policy — the route is the gate — and the spec asserts exactly that gate.

## Authorization readiness handed to downstream consumers (2026-09-15)

`tests/unit/authorization-readiness-consumers.spec.ts` closes the gap left by
`tests/unit/authorization-schema-recovery.spec.ts`: the bootstrap thunk recovers, but the readiness
the wiring RETURNS was derived from it once, so a first-attempt failure was inherited permanently
by everything chaining off it - including the queued-principal capture that
`TicketService.createTicket` performs on every authenticated ticket.

The defective boundary is a pool acquire lost under real contention, and it runs for real: a
disposable `postgres:16-alpine` container, a genuine `pg` Pool with `max: 1` and a 500 ms acquire
timeout whose only client the test holds, the production GUC wrapper, and the real locked-DDL
bootstraps. The failure is a real `timeout exceeded when trying to connect` raised out of
`applyLockedSchema`, not an injected error, and recovery is asserted by reading the tables the
retry had to create and the `oshal_queued_application_principals` row the retry had to insert.

The scoped doubles are all OUTSIDE that boundary: the tool catalog and dynamic executor registry
(recorded call counts, so "the tools registered on the retry" is an assertion rather than a mock
return), the `SwarmAppService`/`AppAccessService` ports, and the ticket row store. The ticket store
is doubled deliberately - ticket persistence is not what fails here, and substituting it is what
makes the case assert on the capture that follows the insert. Its real companions are the
ticket-store RLS entries at the top of this table. Red-proven on `1f0978a0`: both cases fail with
`timeout exceeded when trying to connect`, the first raised out of `createTicket`.

The last consumer left permanently dead by that first attempt was local Test Lab schedule POLLING:
schedule reads recovered per operation, but the poll timer was started inside the boot attempt's
`.then()`, so one lost acquire stopped scheduling until the process restarted. The two polling cases
in `tests/unit/test-lab-schedule-wiring.spec.ts` close it. They inject the failure at the real seam -
the readiness the controller hands the wiring - with the same `timeout exceeded when trying to
connect` PostgreSQL raises, and run the wiring, the schedule schema bootstrap with its advisory-lock
DDL, the store's claim transaction and the service's own poll timer for real, asserting the claim
SQL a later cycle issued. The pg pool is the one scoped double, because a real one cannot
fail-then-recover without a server; its real companion is the disposable-PostgreSQL file above.
Red-proven against the unmodified wiring: the recovery case fails with the readiness asked exactly
once, while the shutdown case passes on both sides.

Not covered: the user-directory and Jarvis-briefing routes are proven at their readiness seam only,
not driven over HTTP.

## Store persistence activation after a lost connection at boot (2026-09-15)

`tests/unit/store-persistence-recovery.spec.ts` covers the boundary that failed on the 00:25:11Z api
boot: the task store, the message store and the memory layer each lost a pool acquire inside the
api's own migration and provisioning burst, fell back to an in-memory Map, ended and nulled their
pools, and stayed non-persistent for the life of the process.

The boundary runs for real. A disposable `postgres:16-alpine` on an ephemeral loopback port; the
store's OWN private pool, built from the process environment by `createOptionalPostgresPool` exactly
as it is in production; a real connection shortage (a non-superuser role with `CONNECTION LIMIT 1`
whose one connection the test holds, so the store's connect is refused by PostgreSQL rather than by
an injected error); the production GUC wrapper; and the real idempotent DDL. Recovery is asserted by
reading the row back out of `chat_tasks` over a separate connection - a store that never recovered
leaves no table at all. A second case repeats it for the injected-pool family
(`PostgresSwarmEscalationStore` with a `max: 1` pool whose only client is held), and a third points
the store at a closed port to prove the degrade still lets the process serve.

The fixture role carries `BYPASSRLS`, which is the legacy single-role posture
`buildOwnerRlsPolicyStatements` documents itself as inert under. That is deliberate: it keeps the
case measuring the connection boundary instead of re-testing RLS, whose real companions are the
ticket-store and authorization RLS entries above.

`tests/unit/persistence-mode-readiness.spec.ts` is the scoped-double companion. Its
`createPersistenceActivation` cases substitute the `activate` callback and the pool, because the
variable there is the retry arithmetic (one shared in-flight attempt, a dropped failed attempt, the
cooldown) and the reporting surface (`/api/readiness` `persistence` leg), not the database. It is
NOT closure evidence for the database seam; the file above is.

## Store package: sports-edge player-weeks (2026-09-16)

`sports_fantasy_player_weeks` is a new table in the `oshal-applications` sports-edge package. The
defect it exists to prevent was a ROUTE that never read a scoring history — the lineup advisor
served the highest-projected lineup every time because `pointsHistory` had no producer anywhere in
the package — so the regression guard has to cross the route, and it does:
`sports-edge/tests/sports-fantasy-history.test.js` drives the real compiled `GET /fantasy/lineup`
and fails on either revert of the wiring.

The database round trip inside that route is DOUBLED, and the double sits inside the boundary rather
than outside it. It is honest about what it is: the pool records the `INSERT INTO
sports_fantasy_player_weeks` parameters the store actually sent, decodes them four at a time in the
order the store builds them, and answers the `SELECT` under the same season / week / roster bounds.
That is enough to prove the route's wiring and nothing more. A double cannot reject a statement
PostgreSQL would reject.

**The real companion exists and passed.** `sports-edge/tests/postgres/player-weeks-contract.mjs`
was run on 2026-09-16 against a disposable `postgres:16-alpine` on `127.0.0.1:55491` (never
`oshal-local-db`; the container was destroyed after the run): the shipped
`migrations/005-sports-fantasy-player-weeks.sql`, `ensureFantasySchema` re-run over the migrated
schema, 1,007 rows written across 3 chunks and counted back over a second connection, the
`ON CONFLICT` upsert re-applied with the row count unchanged, the JSONB round trip, the
week-exclusive / prior-season / roster bounds, and the empty-roster short circuit — seven clauses,
236 ms, all PASS. Self-validated per rule 2: deleting the `ON CONFLICT (season, week, player_id)
DO UPDATE` clause turns it red with `duplicate key value violates unique constraint
"sports_fantasy_player_weeks_pkey"`, so the server is genuinely enforcing rather than the fixture
agreeing with itself.

**What is outstanding is the wiring, not the test.** The sports-edge store-ci job is a
dependency-free plain-node contract — `node --test "tests/*.test.js"` with no framework checkout, no
`npm install` and no service container — so it has neither a `pg` driver nor a database. A case that
SKIPS without one would be worse than this entry: the store's local gate exits non-zero on a skip,
and the sanctioned-skip list in that repository's `CONTRIBUTING.md` does not include this package.
The contract therefore lives in `tests/postgres/`, outside the job's glob, where it cannot pass
vacuously. The done-when for closing this row is `sports-edge/BACKLOG.md` entry F: a PostgreSQL
service container on that job, a step that runs the contract, the env var taught to
`scripts/store-ci-local.mjs` as a capability, and the sanctioned-skip list extended to name it.

This table has no RLS and no owner column by design — it is a shared, league-agnostic cache of raw
stat lines keyed by `(season, week, player_id)`, the same posture as the existing
`sports_fantasy_projections`. The tenancy boundary is therefore not what this row is about; the
statement boundary is.

## Inline chat spend on the windowed budget ledger (2026-09-18)

`tests/unit/inline-chat-cost-ledger-postgres.spec.ts` covers the boundary that failed for the
BACKLOG entry "Inline chat spend is invisible to windowed budget enforcement": the FORCE-RLS'd
`oshal_cost_events` ledger that `BudgetService` sums for its trailing-window caps, which no
controller-inline chat turn ever wrote. Measured read-only on the running box before the fix: 384
owner-attributed inline tasks, 2276 requests, $162.06 in `chat_tasks`, zero ledger rows.

The boundary runs for real. A disposable `postgres:16-alpine` on an ephemeral loopback port with
migrations 078, 090 and 112 applied as written; a `NOSUPERUSER NOBYPASSRLS` runtime role behind the
production GUC wrapper (`wrapPoolWithGuc`), so every statement carries the request identity the way
the api's main pool does; the REAL `TaskOrchestrator`; the REAL app-layer binding
(`createInlineTurnCostLedger` -> `CostTrackingService.recordLedgerEvent`); and the REAL
`BudgetService.computeSpend` / `checkBudget` / `computeSpendByUnit` reads. The turn is issued inside
`runWithRequestIdentity` exactly as `POST /api/send-message` issues it. Self-validated per rule 2:
`relforcerowsecurity` is asserted true and an insert whose `owner_sub` disagrees with the connection
identity is refused with SQLSTATE 42501, so the fixture is enforcing rather than agreeing with itself.
Mutation-proven: with `main`'s orchestrator checked out in place of the fixed one, three of the five
cases go red (spend stays 0, the HARD cap does not refuse, the ledger is empty).

Doubled, outside the boundary: the LLM provider (a stub with an explicit per-turn price — the claim
is where cost lands, not how a vendor prices it), the message store and the stream manager. The
task store is the real `InMemoryTaskStore` in memory-only mode via
`tests/helpers/jarvis-session-task-store.ts`; `chat_tasks` is not the table under test.

`tests/unit/inline-turn-cost-ledger.spec.ts` is the scoped-double companion: the pure event builder,
the ADR-127 unit classification table, the by-unit fold and the run-trace label rendering. It is not
closure evidence for the database seam; the file above is.

## Chart render guards from the Docker Desktop Kubernetes install (2026-09-21)

These three guards render the REAL chart with the helm binary. A render is still not evidence
about a kubelet, an API server or a running Prometheus. Their real companion is the live Docker
Desktop Kubernetes install run on 2026-09-21 on the operator's explicit direction (kind
provisioning, v1.36). Its measured result is in the
[remote-cluster work package](../k8/remote-cluster-work-package.md) status note.

| Boundary audited | Mock/stub disposition | Required real companion | Status |
|---|---|---|---|
| `tests/unit/chart-readiness-probes.spec.ts` (ArangoDB and speaker-diarization readiness probes that could never pass, so `helm --wait` timed out) | No double. The chart is rendered by helm. The diarization half is read from the service's own Python source: key header, key env, allowlist env, `DEFAULT_ALLOWED_HOSTS`, and `/health` calling `_authenticate`. The render cannot show a kubelet probe succeeding against a running ArangoDB or speaker service. | The live Docker Desktop Kubernetes install run on 2026-09-21: the full fleet (30 pods) reached all-Ready only after both probe fixes. | Real companion run once, 2026-09-21. |
| `tests/unit/chart-shared-env-extra.spec.ts` (`swarm.extraEnv` reaching every runtime, and the Docker Desktop overlay's `APP_PACKAGE_DYNAMIC_ROUTES`) | No double. Both roles are rendered by helm. The flag's name and its accepted values are read from the `src/` file that raises "Protected application routes require …=1". Every key the chart already sets in the ConfigMap, set again through `swarm.extraEnv`, must fail the render and be named. The render cannot show the api reading the ConfigMap. | The live Docker Desktop Kubernetes install run on 2026-09-21: every protected store app failed activation closed until `APP_PACKAGE_DYNAMIC_ROUTES=1` reached the api. | Real companion run once, 2026-09-21. |
| `tests/unit/chart-monitoring-parity.spec.ts` (`deploy/monitoring` parity with `ops/monitoring`) | Scoped doubles OUTSIDE the boundary: `kubectl` and `helm` are recording stand-ins first on a minimal PATH, with a nonexistent context and kubeconfig, when the real `install-monitoring.sh` runs. The script's own generation of the PrometheusRule from `alert-rules.yml` runs for real, and so does its refusal of arguments: with `--help` or any other argument, neither stand-in may be called. Rule job selectors are read as both `job="..."` and `job=~"..."`. Prometheus pod discovery and Alertmanager routing are evaluated by the spec, not by Prometheus or Alertmanager. | The live Docker Desktop Kubernetes install run on 2026-09-21: kube-prometheus-stack 91.4.1 scraped 22 oshal targets (1 core + 21 bots), all up, with the five `Swarm*` rules loaded. | Real companion run once, 2026-09-21. |

## Chart guards from the remote-cluster work package (items 2-5)

These guards render the REAL chart with the helm binary, and where a claim is about a script or a
source file they run or read that script or file. None of them reaches a kubelet or an API
server. Their real companions need a cluster and are listed in each row as owed, not run.

| Boundary audited | Mock/stub disposition | Required real companion | Status |
|---|---|---|---|
| `tests/unit/chart-bootstrap-env.spec.ts` (`rbac.botLauncher=false` crash-looping the api: its bootstrap ran `provision-app-role.mjs` without `BOT_DATABASE_URL`) | No double. The env the bootstrap needs is derived from the rendered command (`$NAME`, `${NAME}`, `process.env.NAME`, minus what the command assigns itself) and from `provision-app-role.mjs` (every URL `runtimeCredentials()` passes to `parsePostgresUrl`, traced through `main()` to its `process.env` names). The provision line is cut out of the rendered command, given `--dry-run`, and run by a real POSIX shell and the real script with exactly the env the render supplies. The same run with `BOT_DATABASE_URL` removed must refuse and name it. A dry run connects to no database. | A `helm install --set rbac.botLauncher=false` on a cluster, with the api reaching Ready and `provision-app-role.mjs` printing its provisioned result in the api log. | Guard green 2026-09-21. Red with the fix reverted: 4 failed, 3 passed, including `[provision-app-role] FAILED: BOT_DATABASE_URL is required`. The cluster run is owed. |
| `tests/unit/chart-credentials.spec.ts`, plus the credential cases in `tests/unit/chart-shared-env-extra.spec.ts` (`JWT_SECRET` and the ArangoDB root credentials rendered into a ConfigMap; the `oshal_app` DSN hardcoded with no values path) | No double. Four postures are rendered by helm (defaults, the Docker Desktop overlay, the bot-pod example, every optional switch on). Every ConfigMap is checked by key name and by value. The values are every credential-named values leaf that is not a Secret name the render references, the dev passwords read out of `provision-app-role.mjs`, and any URL with a password in it. Consumers are resolved through the Secrets the same render creates. The extraEnv cases take their credential names from the rendered Secrets and from the names `values.yaml` tells an operator to keep in a Secret. The render cannot show a kubelet mounting those Secrets. | A cluster install where the api, a chart bot and ArangoDB start with the credentials from `oshal-shared-secret` and `oshal-db-credentials`, and `kubectl get configmap oshal-shared-env -o yaml` shows no credential. A bot launched at runtime is a separate check: the launcher's own `envFrom` does not name `oshal-shared-secret`. | Guard green 2026-09-21. Red with the fix reverted: 12 failed, 20 passed across the three affected specs. The cluster run is owed. |
| `tests/unit/chart-vault-server.spec.ts` (the chart ran `vault server -dev`, shipped `devRootToken` and put that root token on the api) | No double. Defaults and the Docker Desktop overlay are rendered by helm. The dev root token is read from `docker-compose.oshal-local.yml`. The token and address env names, the 503 on a missing token and the `sealed` status read are all read from `src/`. The Vault config is read from the ConfigMap the rendered `-config` path points at. Probe tolerance is checked against Vault's documented `sys/health` codes (503 sealed, 501 uninitialized); no Vault server runs. | On a cluster: `oshal-vault-0` Ready while sealed, `vault operator init` and `unseal`, then a delete of the pod showing that the data survives and Vault comes back sealed. The api's console answers 503 `vault_not_configured` throughout. That is the Helm half of "Production Vault hardening"; its issue/use/revoke clause needs a token path this chart does not have (no AppRole login in `src/`). | Guard green 2026-09-21. Red with the fix reverted: 11 failed, 6 passed across it and `chart-infra-parity.spec.ts`. The cluster run is owed. |

## Rules for future fixes

1. Name the failed boundary in the test header and name what remains doubled.
2. Self-validate the fixture where bypass is possible: prove RLS is enforcing, the resolver loaded
   from outside the repo module, or the artifact probe turns red when the file is removed.
3. Put non-optional live/database guards in a required suite. Missing environment is a failure, not
   a skip, unless the outcome is explicitly retained as externally blocked in the active backlog.
4. When a scoped mock remains, link its real companion here. When the companion closes, move the
   completion narrative to durable evidence and remove the item from the active queue.
