# Backlog closeout — 2026-09-22

This is a dated inventory, not a claim that the backlog is finished. The authoritative
queues remain [the core backlog](../BACKLOG.md), [the generated operator queue](../OPERATOR-QUEUE.md),
and each store package's `BACKLOG.md`.

## Reconciled state

- Core `main` was at `b0d6e9bd` before this documentation change. It had no open PRs.
  Store `main` was fast-forwarded to `b3f48a6`; seven store PRs (#243–#249) were open.
- The core queue now has **290 open entries**: 112 actionable, 69 needing the operator,
  46 needing live proof, 11 externally blocked, and 52 untriaged. No entry is currently
  marked `IN PROGRESS`; that is a queue label, not a claim about independent sessions.
  The store has 15 package-level `BACKLOG.md` files outside generated output; their
  work is not included in the core count.
- Three entries that were still counted as open despite completed acceptance moved
  into the [closed ledger](closed-ledger.json): ONNX process-handler isolation
  ([#592](https://github.com/emeraldcoastsystemsgroup/oshal/pull/592)), provider-switch
  precedence ([#633](https://github.com/emeraldcoastsystemsgroup/oshal/pull/633)), and
  scheduled-task repointing ([#696](https://github.com/emeraldcoastsystemsgroup/oshal/pull/696)).
  The two repointed scheduler actions name `C:\Projects\oshal` and both returned 0
  on September 22 (keepalive 22:23:54, Signal Labeler 16:30:01). Evidence-Nightly
  intentionally continues in the private archive, as its [runbook](../runbooks/scheduled-tasks-trunk-vs-archive.md)
  explains.
- The September 17 [handover](session-handover-2026-09-17.md) is historical:
  core authorization [#605](https://github.com/emeraldcoastsystemsgroup/oshal/pull/605),
  Little Monsters roles [store #237](https://github.com/emeraldcoastsystemsgroup/oshal-applications/pull/237),
  and core #620–#623 are merged. The [release record](../releases/little-monsters-person-roles-2026-09-17.md)
  documents the preview deployment, migration 145, installed `student`/`admin`
  assignments, and registered tests. The student's own app navigation and issuer
  self-heal are still awaiting observed acceptance; that item remains in the
  [operator queue](../OPERATOR-QUEUE.md).

## Acceptance still open

- The [application access Phase 2](../BACKLOG.md) item requires a deployed canary and
  exact-method deny/write/refusal proof. A merged implementation or preview role
  assignment alone does not close its broader done-when.
- [Federal CRM](government-contracting-crm.md) still needs installed website-origin
  intake, native linked-deadline acceptance, and the unified obligations dashboard.
  [Create](https://github.com/emeraldcoastsystemsgroup/oshal-applications/blob/main/create/BACKLOG.md)
  has an installed manual image editor, with region-directed AI editing and a video
  timeline remaining. These are application outcomes, not part of the core count.
- The remote-client full-suite flake entry retains its stated 20 consecutive full
  unit-suite run acceptance. Its focused 20-run proof does not meet that condition.
  The lazy-DDL parity, connection-expiry warning, and data-model export follow-ups
  are `actionable`, with no active claim
  found in this checkout.
- Local Docker Desktop's Linux-engine pipe was absent and `localhost:35457/health`
  timed out during this audit. No current container health, migration, installed
  Test Lab run, or real-user acceptance is claimed from this session. The next
  runtime pass should recover the engine, read deploy parity and app status, then
  execute only the particular live proofs above against their stated criteria.

The short-lived [operator-now source](operator-now.json) and generated queue were
refreshed. The Google billing decision remains open, but its September 18 fleet
value is no longer presented as today's observed value.
