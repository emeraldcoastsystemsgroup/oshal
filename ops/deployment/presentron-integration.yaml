# =============================================================================
# LEGACY — DO NOT DEPLOY
# Part of the quarantined pre-chart Kubernetes generation.
# This Deployment runs presentron:latest, an image nothing in this repo builds, in the
# legacy oshal namespace, and reads its key from the legacy oshal-secrets Secret.
# The current Kubernetes path is the Helm chart at deploy/helm/oshal: read
# docs/k8/README.md and docs/adr/129-codeless-k8s-install-path.md. Kept, not deleted,
# while delete-vs-quarantine is still open (docs/k8/remote-cluster-work-package.md,
# item 9).
# =============================================================================
# OSHAL Presentron Kubernetes Integration
# =============================================================================
# CHANGE LOG
# -----------------------------------------------------------------------------
# SEQ                 | AUTHOR                      | DESCRIPTION
# -----------------------------------------------------------------------------
# 1 | maintainer@emeraldcoastsystemsgroup.com   | Initial Presentron deployment scaffold
# 2 | maintainer@emeraldcoastsystemsgroup.com   | Added oshal namespace alignment, service exposure, and oshal secret contract
# 3 | maintainer@emeraldcoastsystemsgroup.com   | Quarantined as legacy (remote-cluster work package item 9): LEGACY — DO NOT DEPLOY banner pointing at the Helm chart deploy/helm/oshal and docs/k8/README.md. No manifest content changed. Guard: tests/unit/k8s-legacy-quarantine.spec.ts.
# =============================================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: presentron
  namespace: oshal
  labels:
    app: presentron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: presentron
  template:
    metadata:
      labels:
        app: presentron
    spec:
      containers:
        - name: presentron
          image: presentron:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: PRESENTRON_API_KEY
              valueFrom:
                secretKeyRef:
                  name: oshal-secrets
                  key: PRESENTRON_API_KEY
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: presentron
  namespace: oshal
spec:
  selector:
    app: presentron
  ports:
    - name: http
      port: 8080
      targetPort: 8080
