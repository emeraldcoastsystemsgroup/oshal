apiVersion: v1
kind: Service
metadata:
  name: oshal-test-app
  namespace: oshal-demo
spec:
  selector:
    app: oshal-test-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
