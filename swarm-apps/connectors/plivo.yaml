provider: plivo
displayName: Plivo
version: 1.0.0
baseUrl: ${env:PLIVO_BASE_URL:-https://api.plivo.com/v1}
auth:
  type: basic
rateLimit:
  burst: 10
  perSecond: 10
retry:
  maxRetries: 3
  honorRetryAfter: true
resources:
  - name: account
    tool: plivo-account
    method: GET
    path: /Account/{authId}/
  - name: messages
    tool: plivo-messages
    method: GET
    path: /Account/{authId}/Message/
    query:
      limit: '{limit}'
      offset: '{offset}'
  - name: numbers
    tool: plivo-numbers
    method: GET
    path: /Account/{authId}/Number/
    query:
      limit: '{limit}'
      offset: '{offset}'
metadata:
  description: "Plivo — reads account, messages, numbers via api.plivo.com. Bring your own key: each user pastes their own token, held per-user in the encrypted connection store."
  category: "Communications"
  iconVerified: false
  iconSource: favicon-fallback
  iconUrl: https://icons.duckduckgo.com/ip3/plivo.com.ico
